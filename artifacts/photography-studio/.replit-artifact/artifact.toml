kind = "web"
previewPath = "/"
title = "Photography Studio"
version = "1.0.0"
id = "artifacts/photography-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25469

[services.development]
run = "pnpm --filter @workspace/photography-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/photography-studio", "run", "build" ]
publicDir = "artifacts/photography-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25469"
BASE_PATH = "/"
